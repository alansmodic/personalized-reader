{
    "name": "alansmodic/personalized-reader",
    "description": "Conversational reader agent for WordPress. Dev tooling only — the plugin itself has no runtime composer dependencies.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.9",
        "wp-coding-standards/wpcs": "^3.1",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "sort-packages": true
    },
    "scripts": {
        "lint": "phpcs",
        "lint:fix": "phpcbf",
        "lint:syntax": "find personalized-reader.php includes blocks -name '*.php' -print0 | xargs -0 -n 1 -P 4 php -l"
    },
    "scripts-descriptions": {
        "lint": "Run WordPress Coding Standards (errors fail).",
        "lint:fix": "Auto-fix what WPCS knows how to fix.",
        "lint:syntax": "Run php -l on every .php file in the repo."
    }
}
